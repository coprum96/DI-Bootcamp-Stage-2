<div class="posts">
    <% posts.forEach(function(post) { %>
      <div class="post">
        <a href="<%= post.link %>"><%= post.title %></a>
        <p><%= post.pubDate %> | <%= post.creator %> | <%= post.category %></p>
        <p><%= post.content %></p>
      </div>
    <% }) %>
  </div>