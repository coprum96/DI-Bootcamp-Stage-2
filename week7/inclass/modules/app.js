const name = 'Mike Tylor';

const greetings = (name) => {
    console.log(`hello, ${name}, welcome to Node.js`)
}

const hello = (name) => {
    console.log(`hello, ${name}, welcome to Node.js`)
}
module.exports = {
 greetings,
 hello
}

